# Comma-separated username:password pairs (hashed at startup)
SEED_USERS=dad:changeme,abby:apples
# Secret used to sign the session cookie
SESSION_SECRET=change-this-to-a-long-random-string
PORT=3000
