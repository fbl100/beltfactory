{
  "//": "Copy to .mcp.json. Local codeindex server — no auth (loopback is the boundary).",
  "mcpServers": {
    "codeindex": {
      "type": "http",
      "url": "http://127.0.0.1:8080/mcp/"
    }
  }
}
